# Used by `ntn workers exec ... --local` after `ntn workers env pull`.
# In production, set these with `ntn workers env set`; never commit their values.
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=

# Optional. Used when Notion first creates the managed sync database.
NOTION_DATABASE_NAME=WHOOP Daily DB
